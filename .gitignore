# Data: raw downloads (re-fetch via API); processed outputs live in results/
data/
test_images/

# Processed rasters in results/ are committed (plan: store all processed images)
# Ignore .tif outside results/; track results/**/*.tif. Optional: Git LFS for *.tif if repo grows.
*.tif
*.tiff
!results/**/*.tif
!results/**/*.tiff

# Credentials
.env
*.pem

# Venvs
venv_cdse/
venvs/

# Slurm
*.out
*.err

# IDE / OS
.cursor/
.DS_Store
